package calendar_app;

import java.sql.SQLException;

public class Driver {
	
	public static void main(String[] args) throws Exception
	{
		
	 CalendarUI calendar;
      if(args.length==0)
      {
		calendar = new CalendarUI(true); 
      }
      else
      {
    	  calendar=new CalendarUI(false);
      }
		calendar.display(); 
	
	}

}
